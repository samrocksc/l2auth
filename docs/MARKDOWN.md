This page utilizes markdown for its codeblocks.

- utilize the <wc-markdown></wc-markdown> web-component for displaying markdown content
- Keep the existing format of any markdown.  It will not render properly if formatted.

Example import of wc-markdown:

```html
<script
  type="module"
  src="https://cdn.jsdelivr.net/gh/vanillawc/wc-markdown@1/index.js"
></script>
```
